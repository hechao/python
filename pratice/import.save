import bs4

root_url = 'http://www.amazon.com'
index_url = root_url + '/'

def url():
    response = requests.get(index_url)
    soup = bs4.BeautifulSoup(response.text)
    return soup

print url()
print url().title
print url().head

